const Constants = {
  app: {
    appName: process.env.REACT_APP_NAME || 'Ant Admin',
  },
  storage: {
    TOKEN: 'ONE-STUDIO-TOKEN',
  },
  api: {
    ARTICLES: 'articles',
    SELECT: 'select',
  },
};

export default Constants;
